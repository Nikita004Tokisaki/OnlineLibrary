<Page x:Class="OnlineLibrary.Views.CatalogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:OnlineLibrary.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Каталог книг" Background="#F0F2F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="White" Padding="15" CornerRadius="8" Margin="0,0,0,15">
            <WrapPanel>
                <TextBox x:Name="txtSearch" Width="200" Height="32" Margin="0,0,10,0" ToolTip="Поиск"/>
                <Button x:Name="btnSearch" Content="Поиск" Style="{StaticResource PrimaryButton}" Width="70" Height="32" Margin="0,0,15,0" Click="BtnSearch_Click"/>

                <TextBlock Text="Сортировка:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="cmbSort" Width="140" Height="32" Margin="0,0,15,0" SelectionChanged="CmbSort_Changed">
                    <ComboBoxItem Content="По названию" IsSelected="True"/>
                    <ComboBoxItem Content="По оценке"/>
                </ComboBox>

                <TextBlock Text="Жанр:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="cmbGenre" Width="140" Height="32" Loaded="CmbGenre_Loaded" SelectionChanged="CmbGenre_Changed"/>
            </WrapPanel>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <WrapPanel x:Name="booksPanel"/>
        </ScrollViewer>
    </Grid>
</Page>
