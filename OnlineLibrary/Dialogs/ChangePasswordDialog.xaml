<Window x:Class="OnlineLibrary.Dialogs.ChangePasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OnlineLibrary.Dialogs"
        mc:Ignorable="d"
        Title="Смена пароля" Height="250" Width="350" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Border Background="White" CornerRadius="10" Padding="20">
        <StackPanel>
            <TextBlock Text="Новый пароль:" FontWeight="Bold"/>
            <PasswordBox x:Name="txtPassword" Height="30" Margin="0,5,0,0"/>
            <TextBlock Text="Подтверждение:" FontWeight="Bold" Margin="0,10,0,0"/>
            <PasswordBox x:Name="txtConfirm" Height="30" Margin="0,5,0,0"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button x:Name="btnSave" Content="Сохранить" Style="{StaticResource PrimaryButton}" Width="100" Click="BtnSave_Click"/>
                <Button x:Name="btnCancel" Content="Отмена" Style="{StaticResource DangerButton}" Width="100" Margin="10,0,0,0" Click="BtnCancel_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
